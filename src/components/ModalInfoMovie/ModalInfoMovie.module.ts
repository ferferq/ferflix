import styled from 'styled-components';

export const InfoMovie = styled.div`
      position: absolute;
      top: 0;
      transform: scale(1.4);
      color: white;
      background: #181818;
      z-index: 10;
      border-radius: 4px;

      p{
        padding: 0 2%;
        font-size: 0.7rem;
      }
`
export const Img = styled.img`
  width: inherit;
  cursor: pointer;
  z-index: 9;
  border-radius: 4px;
`